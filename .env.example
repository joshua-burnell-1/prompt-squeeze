# ABOUTME: Example env config for prompt-squeeze. Copy to .env and fill in.
# ABOUTME: ANTHROPIC_API_KEY only used for the eval harness (Claude-as-judge); not required at runtime.

ANTHROPIC_API_KEY=sk-ant-placeholder
PROMPT_SQUEEZE_LOG_DIR=~/.claude/prompt-squeeze
